These dotfiles assume you're using MacVim with the Janus collection of vim
plugins: https://github.com/carlhuda/janus locally.

INSTALL

git submodule init; git submodule update;


LOCAL INSTALL

cd into this directory and run ./local-install.sh


SERVER INSTALL

cd into this directory and run ./server-install.sh
