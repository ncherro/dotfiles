# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# Compiled Python files
*.pyc

# Compiled C++ files
*.out

# Gutentags
tags
tags.lock
tags.temp

.vscode*

.plugins

*.local.json

# Git worktrees, which live inside the repo they belong to (see
# WORKTREES_SUBDIR in zsh/tmux-workflows.zsh). Ignored globally so the main
# checkout never reads as dirty, and so grep/rg/find skip them by default.
.worktrees/
