config defaultToCurrentScreen true
config nudgePercentOf screenSize
config resizePercentOf screenSize
config windowHintsShowIcons true
config windowHintsIgnoreHiddenWindows false
config windowHintsSpread true

# Hints
bind tab:cmd hint

# Push Bindings
bind l:ctrl;cmd push right bar-resize:screenSizeX*.5
bind h:ctrl;cmd push left  bar-resize:screenSizeX*.5
bind k:ctrl;cmd push up    bar-resize:screenSizeY/2
bind j:ctrl;cmd push down  bar-resize:screenSizeY/2

# Full screen
bind f:ctrl;cmd push left  bar-resize:screenSizeX

# Focus Bindings
bind up:cmd;alt   focus behind
bind down:cmd;alt focus behind
bind k:cmd;alt focus 'Google Chrome'
bind j:cmd;alt focus 'Terminal'
bind h:cmd;alt focus 'Slack'
bind p:cmd;alt focus 'Preview'
bind o:cmd;alt focus 'Finder'
bind y:cmd;alt focus 'Spotify'


# Window Hints
bind esc:cmd hint

# Toggle applications
bind j:ctrl;cmd toggle 'Terminal'
bind k:ctrl;cmd toggle 'Google Chrome'

# Reload slate
bind 1:ctrl relaunch
